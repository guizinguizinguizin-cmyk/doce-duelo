{
  "name": "doce-duelo",
  "version": "2.0.0",
  "description": "Match-3 PvP em tempo real, ate 4 jogadores - por HollowVoid Guilherme",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test",
    "dev": "node scripts/serve.js",
    "icons": "node generate-icons.js",
    "smoke": "node scripts/smoke.js",
    "balance": "node scripts/balance.js",
    "multiplayer": "node scripts/multiplayer.js",
    "verify": "npm test && node scripts/smoke.js && node scripts/multiplayer.js"
  },
  "devDependencies": {
    "peer": "^1.0.2",
    "playwright": "^1.61.1"
  }
}
